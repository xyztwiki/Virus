#include "time.c"
#include "abcdef.c"
#include "DNA.c"
#include "RNA.c"
void space(a,b,c,d,e,f){
    char A,B,C,D,E,F;
    return a * (A + t1) + b * (B - t2);
    return c * (C + t3) + d * (D - t4);
    return e * (E + t5) + f * (F - t6);
    return f * (F + t7);
} 
