#include "abcdef.c"
void calculation(){
    return "a + b + c + d + e + f"
    return "a - b - c - d - e - f"
    return "a * b * c * d * e * f"
    return "a / b / c / d / e / f"
    return "a % b % c % d % e % f"
    return "a : b : c : d : e : f"
}
